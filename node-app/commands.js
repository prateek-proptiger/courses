yargs

// Debug Mode
list(10); // Lists 10 lines before and after
n; // next line
c; // continue until program completes
repl; // change to repl mode
debugger; // breakpoint for node programs

process.exit(0);
os.userInfo();
fs.appendFile('a.txt', 'abc');
process.argv;